# 酒店列表页迁移任务清单

> 目标：按小步可验证方式迁移 `tmc-hotel-list_ryx` 到 H5，避免一次性大改导致上下文或实现失控。
> 原则：每个 Step 完成后更新状态、运行局部校验，再进入下一 Step。

## 执行状态

| Step | 状态 | 范围 | 校验 |
| --- | --- | --- | --- |
| Step 1 | 已完成 | 扩展酒店列表类型、API 请求/响应映射、mock 条件数据 | 已通过 Jacky arm64 Node 方式：`@ryx/shared-types` build、`@ryx/api` build、`@ryx/api` hotel tests |
| Step 2 | 已完成 | 实现 H5 travel-list chrome 的酒店列表外壳，不接复杂筛选 | 浏览器视觉通过；`@ryx/h5` typecheck 被既有跨模块类型错误阻塞，本次酒店列表文件未出现在报错中 |
| Step 3 | 已完成 | 分页、下拉刷新、列表状态恢复 | 浏览器分页验证通过；`@ryx/h5` typecheck 被既有跨模块类型错误阻塞，本次酒店列表分页文件未出现在报错中 |
| Step 4 | 已完成 | 排序与星级/价格筛选 Sheet | 浏览器筛选验证通过；`@ryx/h5` typecheck 被既有跨模块类型错误阻塞，本次酒店筛选文件未出现在报错中 |
| Step 5 | 已完成 | `Condition-Gets` 驱动的位置区域/综合筛选 | 浏览器区域/综合筛选验证通过；`@ryx/h5` typecheck 被既有跨模块类型错误阻塞，本次条件筛选文件未出现在报错中 |
| Step 6 | 已完成 | 酒店卡片字段补齐与详情跳转上下文 | 浏览器视觉与详情 URL 透传通过；`@ryx/h5` typecheck 被既有跨模块类型错误阻塞，本次 Step 6 文件未出现在报错中 |
| Step 7 | 已完成 | 随心住提示、乘客/出差单上下文补齐 | 局部测试、浏览器随心住验证通过；`@ryx/h5` typecheck 被既有跨模块类型错误阻塞，本次 Step 7 文件未出现在报错中 |

## Step 1：数据契约与 API 基础

目标：让 H5 后续 UI 能拿到 Legacy 酒店列表所需字段与筛选元数据。

- [x] 扩展 `HotelListParams`：排序、价格、分类、地理、品牌、主题、服务、设施、出差单、乘客、经纬度、酒店 ID。
- [x] 扩展 `HotelListItem`：`Grade`、`Distance`，并保留 `GreenCloud` / `Tmc` 标签。
- [x] 新增酒店筛选条件类型：`HotelConditionResponse`、`HotelGeo`、`HotelBrand`、`HotelAmenity`。
- [x] `HotelApi` 增加 `getConditions(cityCode)`。
- [x] `buildHotelListRequest` 透传 Step 1 参数，默认 `PageSize = 20`。
- [x] `normalizeHotelListResponse` 映射评分、距离、标签。
- [x] mock 增加 `MOCK_HOTEL_CONDITIONS` 与条件接口 handler。
- [x] 更新 API 单元测试。

校验记录：

- [x] `PATH=/Users/jiangjiankang/.nvm/versions/node/v24.11.1/bin:$PATH ... pnpm --filter @ryx/shared-types build`
- [x] `PATH=/Users/jiangjiankang/.nvm/versions/node/v24.11.1/bin:$PATH ... pnpm --filter @ryx/api build`
- [x] `PATH=/Users/jiangjiankang/.nvm/versions/node/v24.11.1/bin:$PATH ... pnpm --filter @ryx/api test -- hotel.test.ts`

## Step 2：H5 列表外壳

目标：把酒店列表外观先迁到 `DESIGN.md` 的 `H5 Travel List Chrome`，保持基础列表可用。

- [x] 固定渐变 Header：返回、查询摘要、右侧上下文动作。
- [x] 紧凑日期/城市/关键词修改入口。
- [x] 固定底部工具栏，先承载筛选/区域/价格/推荐入口。
- [x] 列表内容改为 full viewport 内部滚动。
- [x] 保留现有城市/日期修改能力。

校验记录：

- [x] Jacky arm64 Node 方式启动 H5 mock：`PATH=/Users/jiangjiankang/.nvm/versions/node/v24.11.1/bin:$PATH ... VITE_API_MODE=mock pnpm --filter @ryx/h5 exec vite --host 127.0.0.1 --port 5173`
- [x] 浏览器检查 `http://127.0.0.1:5173/hotel/list?cityCode=027&cityName=武汉&checkIn=2026-06-30&checkOut=2026-07-01&keyword=光谷`：fixed header、sticky 查询条、底部 toolbar、底部滚动留白均正常，控制台无 error。
- [ ] `@ryx/h5` typecheck：可启动，但被既有跨模块类型错误阻塞（例如 flight passenger `AccountId`、layout `PageToastProps` export、train/order/test 类型债等）；本次新增/修改的酒店列表文件未出现在报错中。

## Step 3：分页与刷新

- [x] 使用 `PageIndex/PageSize` 加载第一页。
- [x] 触底加载下一页并追加。
- [x] 无更多数据状态。
- [x] 下拉刷新或等效刷新入口。
- [x] 日期/城市/关键词变更时重置列表。

校验记录：

- [x] `PATH=/Users/jiangjiankang/.nvm/versions/node/v24.11.1/bin:$PATH ... pnpm --filter @ryx/shared-types build`
- [x] `PATH=/Users/jiangjiankang/.nvm/versions/node/v24.11.1/bin:$PATH ... pnpm --filter @ryx/api build`
- [x] `PATH=/Users/jiangjiankang/.nvm/versions/node/v24.11.1/bin:$PATH ... pnpm --filter @ryx/api test -- hotel.test.ts`
- [x] 浏览器检查 `http://127.0.0.1:5173/hotel/list?cityCode=027&cityName=武汉&checkIn=2026-06-30&checkOut=2026-07-01&keyword=光谷`：首屏 20 条，滚动触底后追加到 25 条，显示“没有更多酒店了”，控制台无 error。
- [ ] `@ryx/h5` typecheck：仍被既有跨模块类型错误阻塞；本次 `HotelListPage` / `useHotelList` / hotel toolbar/header 文件未出现在报错中。

## Step 4：排序与星级/价格

- [x] 推荐排序：低价、高价、星级升、星级降。
- [x] 星级多选。
- [x] 分类多选。
- [x] 价格档与自定义价。
- [x] 筛选 dirty 状态与重置。

校验记录：

- [x] `PATH=/Users/jiangjiankang/.nvm/versions/node/v24.11.1/bin:$PATH ... pnpm --filter @ryx/shared-types build`
- [x] `PATH=/Users/jiangjiankang/.nvm/versions/node/v24.11.1/bin:$PATH ... pnpm --filter @ryx/api build`
- [x] `PATH=/Users/jiangjiankang/.nvm/versions/node/v24.11.1/bin:$PATH ... pnpm --filter @ryx/api test -- hotel.test.ts`
- [x] 浏览器检查：打开「价格星级」Sheet，选择「低价优先」和「300-450」价格档后确认，列表价格按 320、338、356、368、374... 升序展示，底部 toolbar 显示“已筛选”，控制台无 error。
- [ ] `@ryx/h5` typecheck：仍被既有跨模块类型错误阻塞；本次 `HotelListFilterSheet` / `hotel-list-filters` / `HotelListPage` 文件未出现在报错中。

## Step 5：位置区域与综合筛选

- [x] 接入 `getConditions(cityCode)`。
- [x] 位置区域分组与地铁三级结构（类型 / 线路 / 站点）。
- [x] 底部筛选入口归类：推荐/价格星级仅展示推荐排序、星级、酒店类型、价格；区域位置仅展示位置条件；筛选仅展示品牌、主题、服务、设施。
- [x] 区域位置单入口隐藏外层分类栏，第一栏直接展示地铁/地标/兴趣点/商业中心/火车站等位置分类；仅地铁保留线路中栏。
- [x] 区域位置第一栏分类支持选中提示：当该分类下有已选位置时，在分类按钮右上角显示品牌蓝小点。
- [x] 品牌按 Legacy 分类展示：热门品牌、经济、舒适、高端、豪华，支持分组「不限」与多选。
- [x] 主题/服务/设施分组。
- [x] 条件加载失败与空态。

校验记录：

- [x] `PATH=/Users/jiangjiankang/.nvm/versions/node/v24.11.1/bin:$PATH ... pnpm --filter @ryx/shared-types build`
- [x] `PATH=/Users/jiangjiankang/.nvm/versions/node/v24.11.1/bin:$PATH ... pnpm --filter @ryx/api build`
- [x] `PATH=/Users/jiangjiankang/.nvm/versions/node/v24.11.1/bin:$PATH ... pnpm --filter @ryx/api test -- hotel.test.ts`
- [x] 浏览器检查：点击「区域位置」选择「洪山区」后确认，列表均为洪山相关酒店且显示“已筛选”；再次打开「已筛选」选择品牌「亚朵」与设施「健身房」后确认，列表收窄到 7 条，控制台无 error。
- [x] 真实测试接口浏览器检查：北京 `cityCode=1101` 的「区域位置」显示 legacy 对齐的左中右结构，左侧含「地铁 / 地标 / 兴趣点 / 商业中心 / 火车站」，中间含 `10号线`、`4号线` 等线路，右侧含「火器营 / 长春桥 / 车道沟」等站点，控制台无 error。
- [x] 真实测试接口浏览器检查：北京 `cityCode=1101` 的「品牌」显示「热门品牌（可多选）/ 经济（可多选）/ 舒适（可多选）/ 高端（可多选）/ 豪华（可多选）」分组；选择「丽笙」后底部显示“已筛选”并刷新为真实接口空结果，控制台无 app error。
- [x] 真实测试接口浏览器检查：推荐/价格星级/区域位置/筛选入口按三类归类展示，区域位置第一栏直接为地理分类，地铁为三栏结构，地标等非地铁分类为两栏结构；页面刷新后功能正常渲染。
- [x] 真实测试接口浏览器检查：在区域位置选择「地标 / 东方广场」后，第一栏「地标」右上角显示小蓝点，「地铁」不误显示，控制台无 error。
- [ ] `@ryx/h5` typecheck：仍被既有跨模块类型错误阻塞；本次 `HotelListFilterSheet` / `hotel-list-filters` / `useHotelConditions` 相关文件未出现在报错中。

## Step 6：酒店卡片与详情上下文

- [x] 旅发酒店 / 协议酒店标签。
- [x] `Grade >= 4` 评分。
- [x] 星级、地址、距离、价格布局。
- [x] 点击详情透传日期、城市、价格、酒店类型、出差单。

校验记录：

- [x] `PATH=/Users/jiangjiankang/.nvm/versions/node/v24.11.1/bin:$PATH ... pnpm --filter @ryx/shared-types build`
- [x] 浏览器检查 `http://127.0.0.1:5173/hotel/list?cityCode=027&cityName=武汉&checkIn=2026-06-30&checkOut=2026-07-01&keyword=光谷&hotelType=Tmc&travelFormId=TF123`：首屏卡片展示「旅发酒店」「协议酒店」、`4.8分` 等评分、`1.2公里/850米` 等距离；`GreenCloud + Tmc` 同时存在时优先展示「旅发酒店」。
- [x] 点击首张酒店进入 `/hotel/H10001`，URL 透传 `checkIn=2026-06-30`、`checkOut=2026-07-01`、`cityCode=027`、`cityName=武汉`、`minPrice=563`、`hotelType=Tmc`、`travelFormId=TF123`。
- [x] 浏览器 console error 为空。
- [ ] `@ryx/h5` typecheck：仍被既有跨模块类型错误阻塞；本次 `HotelListItem` / `HotelListPage` / `hotel-detail` 文件未出现在报错中。

## Step 7：业务上下文补齐

- [x] 乘客 AccountId 拼接到 `Passengers`。
- [x] 透传 `travelformid`。
- [x] 员工城市 `staffCityCode`。
- [x] 随心住提示与说明弹窗。

实现记录：

- [x] `HotelListPage` 接入 `usePassengerSelection(ProductType.Hotel)`，列表请求按乘客 `AccountId` 去重后拼接到 `Passengers`。
- [x] `HotelListPage` 兼容 `travelFormId` / `travelformid`，列表请求与详情跳转均透传。
- [x] 新增 `hotel-list-context`：集中处理乘客 AccountId、`staffCityCode` localStorage 读取、随心住 TMC 配置兼容判断。
- [x] `TmcInfo` 扩展随心住兼容字段；mock TMC 开启 `AllowHotelOutPolicySelfPay` 便于 H5 验证。
- [x] 列表顶部展示随心住提示，点击打开说明弹窗；`hotelType=Agent` 时不展示。

校验记录：

- [x] `PATH=/Users/jiangjiankang/.nvm/versions/node/v24.11.1/bin:$PATH ... pnpm --filter @ryx/shared-types build`
- [x] `PATH=/Users/jiangjiankang/.nvm/versions/node/v24.11.1/bin:$PATH ... pnpm --filter @ryx/h5 test -- hotel-list-context.test.ts`
- [x] 浏览器检查：`hotelType=Tmc` 时随心住提示展示，点击可打开说明弹窗，点击「我知道了」可关闭；`hotelType=Agent` 时提示隐藏；浏览器 console error 为空。
- [x] 列表请求上下文：`hotel-list-context.test.ts` 覆盖 `Passengers` AccountId 去重拼接、`StaffCityCode` localStorage 读取、随心住兼容字段判断；`@ryx/api` hotel tests 覆盖列表 API payload 透传。
- [ ] `@ryx/h5` typecheck：仍被既有跨模块类型错误阻塞（flight/train/hotel-book 等旧错误）；本次 `HotelListPage` / `hotel-list-context` / `TmcInfo` 相关文件未出现在报错中。
