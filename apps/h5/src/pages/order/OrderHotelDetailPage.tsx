import { usePageHeader } from "@/components/layout";

/** Placeholder for RYX `tmc-order-hotel-detail_ryx` (Wave 4). */
export function OrderHotelDetailPage() {
  usePageHeader({ title: "订单详情", showBack: true });

  return <p className="p-4 text-sm text-[#9CA3AF]">订单详情即将上线</p>;
}
